# Produção fica no `.env`, versionado. Para apontar para outro ambiente, copie este
# arquivo para `.env.local`, que o git ignora e o Vite carrega por cima do `.env`.
VITE_WAVOIP_DEVICES_URL=
VITE_WAVOIP_API_URL=
